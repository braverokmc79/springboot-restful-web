import React, { useContext } from 'react'

const FooterComponent:React.FC = () => {
  
  return (
    <footer className="sticky bottom-0 w-full bg-gray-800 text-white text-center py-4">
      하단
    </footer>
  )
}

export default FooterComponent